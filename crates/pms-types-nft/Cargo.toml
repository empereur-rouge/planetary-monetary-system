[package]
name = "pms-types-nft"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
rust_decimal = { version = "1", features = ["serde", "serde-with-str"] }

[dev-dependencies]
serde_json = "1"
